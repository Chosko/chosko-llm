# Task 213 — Resolve `<id|name|id-name>` in /runbook-clean, /runbook-describe, /pipeline-patch and /pipeline-revise, and update the pipeline-engine routing rows and edge E6

Target: claude

## Goal
Part of feature `runbook-id-filenames` (.claude/domain/features/runbook-id-filenames.md).
The remaining runbook consumers that resolve a runbook argument or open a body are
brought onto the schema's three-form rule and `File:` authority. None of them reads
`body-migration.md` or renames anything. `pipeline-engine`'s routing table and graph,
which name runbook paths and argument notation, follow.

## Acceptance criteria
- `/runbook-clean`: resolution cites the schema's three-form rule (replacing the
  restated all-digits rule, ~line 124); usage/frontmatter notation becomes
  `<id|name|id-name>`; plan examples show prefixed paths; it still deletes whatever
  `File:` names and notes a missing file in its plan; it never migrates.
- `/runbook-describe`: resolution cites the three-form rule (replacing ~line 73); the
  body is read at `File:`, with no mention of `.claude/runbooks/<name>.md` (~line 49);
  it stays read-only. Edits apply to the body as task 207 left it.
- `/pipeline-patch` and `pipeline-revise`: the `runbook=` anchor notation becomes
  `runbook=<id|name|id-name>` everywhere it appears (usage header, argument list, stop
  message, resolution bullet); resolution still cites the schema.
- `routing.md`: the runbook rows' argument notation reads `<id|name|id-name>`, and any
  body path they name reads `File:` rather than `<name>.md`; `/runbook-list`'s row is
  unchanged; `./scripts/check-routing.sh` passes.
- `graph.md` edge E6's line shows `File: .claude/runbooks/<id>-<name>.md` and notes that
  a legacy `<name>.md` value is legal during lazy migration.
- `/runbook-list`, `/pipeline-check` and `lint.md` are not edited.
- Each touched shipped body bumps its own `version:` (patch: `runbook-clean`,
  `runbook-describe`, `pipeline-patch`, `pipeline-revise`, and `pipeline-engine`'s
  `SKILL.md` for its reference files); root `VERSION` gets a patch bump with a matching
  `CHANGELOG.md` section; `./scripts/check-changelog.sh` passes.

## Decisions
- **Preconditions 207, 208, 209, 211.** 207 rewrites `commands/runbook-describe.md`;
  208 and 209 edit `routing.md`'s `/runbook-run` row; 211 writes the schema rule cited.
- **`pipeline-engine`'s `version:` is bumped** even though tasks 208/209 left it alone
  for their row edits — the design's Versioning section says every shipped body
  touched bumps its own version, and this task also edits `graph.md`.
- **Patch bumps, not minor** — for these consumers the change is notation plus
  resolution, no new flag or behaviour of their own.

## Hints
- .claude/domain/features/runbook-id-filenames.md — the design (read-only here);
  § Consumers, § Interfaces and contracts
- commands/runbook-clean.md — frontmatter (~line 5), header (~line 12), resolution
  (~lines 115–125), plan examples
- commands/runbook-describe.md — ~lines 49, 73, 90, 101 (as 207 left them)
- commands/pipeline-patch.md — ~lines 5, 15, 73, 77, 94
- skills/pipeline-revise/SKILL.md — ~lines 5, 15, 90, 115; grep its branch files for
  `runbook=` too
- skills/pipeline-engine/references/routing.md — runbook rows (~lines 81–85); run
  ./scripts/check-routing.sh
- skills/pipeline-engine/references/graph.md — § E6 (~lines 171–180)
- skills/pipeline-engine/SKILL.md — `version:`
- VERSION, CHANGELOG.md — run ./scripts/check-changelog.sh
