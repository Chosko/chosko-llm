# Task 71 — Add push (with --no-push) to refactor-codebase and refactor-tests

Target: claude

## Goal
Apply the commit-and-push protocol from task 68 to refactor-codebase and
refactor-tests — both authoring commands (`--commit`-gated commit today).

## Acceptance criteria
- commands/refactor-codebase.md's commit step (~line 286-290) pulls at
  start when `--commit` is passed, commits as today, re-syncs, and pushes
  per the protocol. `--no-push` documented alongside `--commit`.
- commands/refactor-tests.md's commit step (~line 167-171) gets the same
  treatment.
- `.claude/domain/refactor-workflow.md` gets a brief pointer to the
  authoring guide's protocol.
- README.md's entries for these two commands mention push / `--no-push`.
- Root VERSION bumped (minor).

## Decisions
- Same start-of-pull gating as task 70: only pull when `--commit` was
  actually passed.

## Hints
- commands/refactor-codebase.md — commit step (~line 286-290).
- commands/refactor-tests.md — commit step (~line 167-171).
