# Task 70 — Add push (with --no-push) to context-build and context-update

Target: claude

## Goal
Apply the commit-and-push protocol from task 68 to context-build
(authoring command, commits only under `--commit`) and context-update
(auto-commits by default). Both currently end with a plain `git commit`.

## Acceptance criteria
- commands/context-build.md's commit step pulls at start (only meaningful
  when `--commit` is passed — the pull only needs to run once it's known
  a commit will happen), commits as today, re-syncs, and pushes per the
  protocol. `--no-push` documented alongside `--commit`.
- commands/context-update.md's commit step gets the same treatment;
  `--no-push` added alongside its existing `--no-commit`.
- `.claude/domain/context-workflow.md` gets a brief pointer to the
  authoring guide's protocol near its discussion of the commit step.
- README.md's entries for these two commands mention push / `--no-push`.
- Root VERSION bumped (minor).

## Decisions
- For context-build, run the start-of-workflow pull only when `--commit`
  was passed (mirrors: no commit planned means no reason to sync first).

## Hints
- commands/context-build.md — commit step (~line 184-188).
- commands/context-update.md — commit step (~line 244-247).
