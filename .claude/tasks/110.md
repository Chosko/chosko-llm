# Task 110 — Update documentation for feature slice-aware-architecture

Target: claude

## Goal
Feature `slice-aware-architecture` changes `/architect`'s input resolution and
extends the `FEATURES.md` `Source:` field with an optional milestone
parenthetical. The documentation layers describing that area are now out of
date: the product-workflow domain doc still specifies `Source:` in its old
two-value form, the context layer still describes `skills/architect/` as
having four supporting files, and README.md does not mention slice mode or
`--no-slices`. Bring them into line with the behaviour tasks 108 and 109
shipped.

## Acceptance criteria
- `.claude/domain/product-workflow.md`'s `Source:` field description and its
  who-writes-what table reflect the optional ` (<milestone-slug>)` suffix, and
  the by-line `FEATURES.md` ownership split still reads correctly:
  `/architect` owns `Status:` / `Doc:` / `Source:`, `/task-add` owns `Tasks:`.
- `.claude/context/features.md`'s `skills/architect/` entry lists the two new
  supporting files and their branch condition, and its supporting-file count
  (and any count in `.claude/context/INDEX.md`) is corrected.
- README.md describes slice mode and `--no-slices` at the level it describes
  the pipeline's other stages.
- The feature document
  `.claude/domain/features/slice-aware-architecture.md` is reviewed against
  what actually shipped, and its open questions are resolved or restated —
  in particular how slice scope statements are matched to section headings,
  which task 109 settles. **This file is `/architect`'s, not `/task-add`'s:
  if the review finds a real design divergence rather than a wording gap, say
  so and let the user re-run `/architect` instead of rewriting the design.**
- Documentation-only change beyond the version bump; no skill or script
  behaviour is altered.
- Root `VERSION` bumped patch.

## Hints
- `.claude/domain/product-workflow.md` — `Source:` spec (~line 76, ~line 87)
  and the ownership table (~line 191).
- `.claude/context/features.md` — the `skills/architect/` entry (~line 273
  onward) and the supporting-file count (~line 397).
- `.claude/context/INDEX.md` — the `Last updated` anchor and any row touching
  the architect skill.
- `README.md` — the product-pipeline description.
- `.claude/domain/features/slice-aware-architecture.md` — the originating
  feature document, flagged for review per the ownership note above.
- `docs/authoring-guide.md` — check whether the `Source:` schema is restated
  there; update only if it is.
- `VERSION` — patch bump.
