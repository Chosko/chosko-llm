# Task 74 — Add push (with --no-push) to unity-mcp-setup

Target: claude

## Goal
Apply the commit-and-push protocol from task 68 to unity-mcp-setup's
`--commit`-gated commit step.

## Acceptance criteria
- commands/unity-mcp-setup.md's commit step (~line 301-303) pulls at
  start when `--commit` is passed, commits as today, re-syncs, and pushes
  per the protocol. `--no-push` documented alongside `--commit`.
- README.md's unity-mcp-setup entry mentions push / `--no-push`.
- Root VERSION bumped (minor).

## Decisions
- Same start-of-pull gating as tasks 70-72: only pull when `--commit` was
  actually passed.

## Hints
- commands/unity-mcp-setup.md — commit step (~line 301-303).
