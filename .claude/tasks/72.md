# Task 72 — Add push (with --no-push) to domain-setup, product-design, and architect

Target: claude

## Goal
Apply the commit-and-push protocol from task 68 to the three product-pipeline
authoring commands/skills, each `--commit`-gated today.

## Acceptance criteria
- commands/domain-setup.md's commit step (~line 234-237) pulls at start
  when `--commit` is passed, commits as today, re-syncs, and pushes per
  the protocol. `--no-push` documented alongside `--commit`.
- skills/product-design/SKILL.md's commit step (~line 279-282) and
  skills/architect/SKILL.md's commit step (~line 262-265) get the same
  treatment.
- `.claude/domain/product-workflow.md` gets a brief pointer to the
  authoring guide's protocol.
- README.md's entries for these three features mention push / `--no-push`.
- Root VERSION bumped (minor).

## Decisions
- Same start-of-pull gating as tasks 70/71: only pull when `--commit` was
  actually passed.

## Hints
- commands/domain-setup.md — commit step (~line 234-237).
- skills/product-design/SKILL.md — commit step (~line 279-282).
- skills/architect/SKILL.md — commit step (~line 262-265).
