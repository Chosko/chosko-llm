# Task 31 — Make /project-setup GATHER ask one question at a time and seed CLAUDE.md iteratively

Target: claude

## Goal
The /project-setup GATHER phase currently presents all setup questions in
one message and asks the user to answer them together. Change it to ask one
question at a time, waiting for each answer before asking the next. Also
change the CLAUDE.md seeding step (1b) so that, instead of treating "pasted
nothing" as a decline, it runs an optional iterative input phase: the user
pastes or types information, and Claude invites more ("keep adding, or say
'done' when finished") until the user signals done. Seeding is still skipped
only if the user provides nothing and ends the phase.

## Acceptance criteria
- GATHER asks questions 1a–1e sequentially, one per message, waiting for the
  user's reply before the next.
- The intro no longer tells the user to answer everything in one go.
- Step 1b runs an iterative loop: after each paste/typed entry, Claude offers
  to accept more input or accept "done" to finish.
- If the user ends 1b without providing anything, seeding is treated as
  declined (existing skip behavior preserved).
- tests/smoke/project-setup.md reflects the one-at-a-time flow and the
  iterative seeding loop.

## Decisions
- Keep the existing CONFIRM (one approval) and EXECUTE ordering unchanged —
  only the GATHER interaction style changes.

## Hints
- commands/project-setup.md — GATHER intro (around the "ask the questions in
  order" paragraph) and section 1b.
- tests/smoke/project-setup.md — update the "asks in order" expectations.
