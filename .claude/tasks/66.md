# Task 66 — Run /context-update full to refresh the navigation context layer

Target: claude

## Goal
The navigation context layer under `.claude/context/` was last refreshed
on 2026-07-27. Run `/context-update full` to force a full read/verify pass
across every context file against its current source, independent of git
history, and let it write and commit whatever drift it finds.

## Acceptance criteria
- `/context-update full -y` (or an equivalent interactive run) has been
  executed against this repo.
- Any context file Phase 1 found stale has been updated in place per
  `/context-update`'s own Phase 2 rules.
- `INDEX.md`'s "Last updated" date is bumped to the date the run
  happened.
- The updated context files (plus `INDEX.md`) are committed via
  `/context-update`'s own Phase 3 — no separate manual commit.

## Decisions
- This task exists only to schedule the run as backlog work rather than
  running it ad hoc — `/context-update` already owns detecting scope,
  writing, and committing, so the task body doesn't restate that logic.

## Hints
- .claude/context/INDEX.md
- .claude/context/*.md
