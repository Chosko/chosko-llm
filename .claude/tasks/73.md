# Task 73 — Add push (with --no-push) to project-setup, and document the Plastic push exemption in the injected VCS section

Target: claude

## Goal
Apply the commit-and-push protocol from task 68 to project-setup's Step 4c
(its own `--commit`-gated commit of wizard artifacts), and extend the
fixed `## VCS` section it injects into a Plastic project's CLAUDE.md
(Step 3) with the sentence that documents why the pull/re-sync/push cycle
never applies there — per the user's explicit instruction, this is
recorded once at project-setup time rather than each command re-deciding
it at runtime.

## Acceptance criteria
- commands/project-setup.md Step 4c (~line 440-457) pulls at start when
  `--commit` is passed, commits as today, re-syncs, and pushes per the
  protocol (git projects only). `--no-push` documented alongside
  `--commit`.
- The fixed Plastic `## VCS` template in Step 3 (~line 338-361) gains one
  more line in its mapping/prose, stating that `git push` has no Plastic
  equivalent because `cm checkin` already syncs the changeset to the
  central server — so commands never run a pull/merge/push cycle in this
  project. Existing "update in place rather than duplicating" behavior for
  an already-present `## VCS` section still applies.
- Since project-setup orchestrates task-setup/domain-setup/context-build/
  unity-mcp-setup with `--commit` (per docs/authoring-guide.md's existing
  "one orchestrator" note), it now also forwards `--no-push` to those
  nested calls when the user passed it to project-setup.
- README.md's project-setup entry mentions push / `--no-push`.
- Root VERSION bumped (minor).

## Decisions
- The Plastic exemption text is fixed template prose added once here, not
  a per-command runtime check — commands already read the `## VCS`
  section for git→cm substitution (task 47), so a command seeing no
  push/pull instructions there simply doesn't perform them.

## Hints
- commands/project-setup.md — Step 3 VCS template (~line 330-361), Step
  4c commit step (~line 440-457), orchestration of nested `--commit`
  calls (~line 173-176 area cross-referenced in docs/authoring-guide.md).
- docs/authoring-guide.md — "one orchestrator" note (~line 173-176).
