# Task 196 — Put the archive rule and the archived-file form in `resolution.md`

Target: claude

## Goal
Feature `task-archive` (.claude/domain/features/task-archive.md) makes a
pruned task's body move to `.claude/tasks/archive/<N>.md` instead of being
deleted, and makes an id that is referenced but absent from `TASKS.md` mean
"archived and terminal". That rule needs exactly one home before any consumer
can cite it. Give it to `skills/task-engine/references/resolution.md`, which
already owns where the backlog lives, the summary-block schema and when a body
file may be opened — the one-authority-per-rule discipline `shared-phase-engine`
set. Every other task of this feature, and tasks 176 and 178 of
`pipeline-engine` (which already cite this file for the rule without restating
it), depend on this landing first.

## Acceptance criteria
- `resolution.md` gains a section on the archive stating its location
  (`.claude/tasks/archive/<N>.md`, the task's id as the file name), that the
  folder is created by the archiving run and is append-only, and the archived
  file's form: the original body unchanged, plus a frozen header of the same
  `Key: value` lines the body already uses for `Target:`, directly under the
  title — `Archived:` (`YYYY-MM-DD`), `Status:`, `Files:`, `Preconditions:`,
  and `Feature:` when the task had one. Nothing else is derived or added, and
  the header is written once and never updated.
- The same section states the rule every consumer will cite, in one sentence:
  an id referenced anywhere but absent from `TASKS.md` is archived and
  terminal; no command probes the folder, opens an archived file, or reports
  on its contents, except when the user names a task and asks to read it.
- It states that absence is the whole signal: no reader verifies the file is
  there, and a hand-deleted body is indistinguishable from an archived one by
  design — the difference surfaces only when the user asks to read the task,
  at which point a missing file says so itself.
- § *Opening a per-task body file* is reconciled with that: the paths it
  governs are `.claude/tasks/<N>.md` only, and nothing in it opens a file
  under `archive/`.
- `/task-list`'s per-consumer note is left as it stands — its existing "must
  NOT open any file under `.claude/tasks/`" already covers `archive/`, and a
  second prohibition would be a second copy.
- The `/task-clean` per-consumer note is rewritten for the skill this feature
  ships: the single reason it opens a body file is to probe that the file
  exists before planning its **move**, not its deletion, and the prune path
  does not open `.claude/FEATURES.md` at all.
- The `/task-add` per-consumer note records that reconciliation leaves an
  absent id unclassified and keeps it when the `Tasks:` line is rewritten.
- `skills/task-engine/SKILL.md`'s map row for `references/resolution.md` names
  the archive alongside what it already owns; no rule text is added to
  `SKILL.md`, and its own `version:` is bumped.
- No other file under `references/` restates any part of the rule.
- Root `VERSION` takes a patch bump and `CHANGELOG.md` gains a matching top
  section; `./scripts/check-changelog.sh` is silent afterwards.

## Decisions
- `resolution.md` gets exactly one writer in this feature. Its per-consumer
  notes are statements of this rule's shape, so splitting them across the four
  tasks that touch each consumer would put four writers on one file for a
  handful of lines. Each consumer still states its own deviation in its own
  body; this file stays the mirror the engine already keeps.
- The rule ships before the skill that produces archives. Tasks 176 and 178 of
  `pipeline-engine` were authored to cite this file for the archived-and-terminal
  rule and to note in their commit if it is not there yet; landing the rule
  first is what closes that gap. Wiring this task's id onto their
  `Preconditions:` lines is a later runbook step's job, not this task's.
- Patch, not minor, on root `VERSION`: a reference file gains a rule and no new
  invocable surface reaches the user until task 197.

## Hints
- skills/task-engine/references/resolution.md
- skills/task-engine/SKILL.md (map row + version bump)
- .claude/domain/features/task-archive.md (read-only — the design; `/architect`
  owns it, editing is not authorised for this task)
- .claude/tasks/176.md and .claude/tasks/178.md (read-only — the two authored
  tasks that already cite this rule; the wording there is the contract)
- VERSION
- CHANGELOG.md
