# Task 180 — Add the repo-local routing check

Target: claude

> Merged into task 175 on 2026-09-11; that body is the authority. This task is `[SKIP]` and awaits `/task-clean`.

## Goal
Add `scripts/check-routing.sh` beside `check-changelog.sh`: an authoring-time
guard that the routing table stays honest. A routing table that names a deleted
skill, or that has no row for a feature which reads the engine, is exactly the
drift `/pipeline-check` exists to catch everywhere else — and nothing would
catch it here. Semantics cannot be checked mechanically; existence can, and this
script checks existence in both directions.

## Acceptance criteria
- `scripts/check-routing.sh` starts with `set -euo pipefail`, sources `lib.sh`
  the way `check-changelog.sh` does, and adds no dependency beyond
  awk / sed / grep.
- Invariant 1: every row in `skills/pipeline-engine/references/routing.md` names
  a feature that exists in this repo — `commands/<name>.md` **or**
  `skills/<name>/SKILL.md`, either kind accepted, since a feature can migrate
  between them. A row naming a feature that exists as neither fails, naming the
  row and both paths it looked for.
- Invariant 2: every shipped feature declaring `requires: skill:pipeline-engine`
  has a row in the table. A consumer without a row fails, naming the feature.
  The check uses `lib.sh`'s existing `requires:` helpers rather than parsing
  frontmatter again.
- The script's parser contract is stated in a comment at the top — which line
  shape it reads as a row and what it ignores — the way `check-changelog.sh`
  states its `^## ` contract, so a later edit to the table's formatting does not
  silently break the check.
- A leading comment states what the script can and cannot prove: existence in
  both directions, never whether a row's Consumes / Produces / Owns entries are
  true, and never that a pipeline feature which does not read the engine has a
  row — that half stays a human's job.
- Silent on success, non-zero with a specific message on failure; it uses
  `lib.sh`'s `die`, matching `check-changelog.sh`'s register exactly.
- The script is repo-local and authoring-time only: it is not a feature, gets no
  frontmatter, is invisible to every CLI verb, and is installed nowhere.
- Root `VERSION` takes a **patch** bump and `CHANGELOG.md` gains a matching top
  section; `./scripts/check-changelog.sh` exits zero.

## Decisions
- The completeness half of the check is scoped to features declaring
  `requires: skill:pipeline-engine` rather than to "every pipeline feature"
  (user decision at planning time). There is no mechanical definition of
  "pipeline feature", and the two candidates for one — an authored membership
  list, or a new frontmatter key — are respectively near-vacuous and new
  machinery CLAUDE.md tells us to avoid. Rows for non-consumers such as
  `/architect` and `/task-add` stay authored and unchecked, which is the
  unavoidable share of the table that is semantics.
- Invariant 1 accepts either kind for a concrete reason: `task-archive` migrates
  `/task-clean` from a command to a skill, and a check that assumed one kind
  would fail on a migration that broke nothing.
- `scripts/` is shipped in the managed clone, so this script does bump root
  `VERSION`. The `.claude/skills/` exemption covers repo-local *skills* only,
  and `check-changelog.sh` set the precedent for a repo-local script under
  `scripts/`.
- `.claude/domain/features/pipeline-engine.md` is read-only in this task.

## Hints
- scripts/check-routing.sh (new)
- scripts/check-changelog.sh (the template: structure, comment register, die
  messages, invariant numbering)
- scripts/lib.sh (`die`, `requires_specs` / `requires_specs_lenient`,
  `read_frontmatter_field`)
- skills/pipeline-engine/references/routing.md (the table being checked)
- .claude/domain/features/pipeline-engine.md (read-only — the design; `/architect`
  owns it, editing is not authorised for this task)
- VERSION
- CHANGELOG.md
- scripts/check-changelog.sh
