# Task 243 — Document the description contract and the hidden-feature set

Target: claude

## Goal
Tasks 240–242 changed three things the documentation layers describe:
`chosko-llm show` now prints the body's `#` header under the description; a
set of features is invisible to the model by design (`disable-model-
invocation`) and `unity-mcp-skill` loads only under `paths:`; and every
description is short by contract, with flags living in the body header. Bring
`README.md`, `docs/reference.md`, the domain layer and the context layer in
line. Documentation only — no `VERSION` bump, no `CHANGELOG` entry.

## Acceptance criteria
- `README.md` (the frontmatter paragraph near line 497 and the `show`
  mention) and `docs/reference.md` (§ `show`; § pipeline-suggest's "its
  description is the whole trigger" passage; any "the description documents
  the flags" wording) reflect the new behaviour and the hidden-feature set.
- `.claude/domain/features/repo-local-audits.md` is reconciled with what
  shipped on exactly these 3 points — editing it is authorised for this
  task, see Decisions:
  1. The thresholds table gains the 1,536-char hard cap and the ` --- `
     signal beside the 30-word flag.
  2. The "Several descriptions in this repo run long enough to matter —
     task-implement's is a paragraph" example is replaced by the contract
     and the measured before/after.
  3. The `description:` rationale paragraph cites the harness truncation
     rule as the reason the signal exists.
  Nothing else in the document changes.
- `.claude/domain/features/pipeline-suggest.md` is reconciled on exactly
  this 1 point — editing it is authorised for this task, see Decisions:
  1. The trigger-in-description design now carries the 1,000-char /
     150-word budget and trigger-first, "Not for"-last ordering.
  Nothing else in the document changes.
- `.claude/domain/features/shared-phase-engine.md` is reconciled on exactly
  this 1 point — editing it is authorised for this task, see Decisions:
  1. `task-engine`'s "NOT a skill the user invokes" description prose is
     replaced by `disable-model-invocation: true` as the mechanism that
     keeps it out of skill selection.
  Nothing else in the document changes.
- `.claude/context/cmd-show.md` describes the header printing;
  `.claude/context/features.md` describes the description contract, the
  loading-control keys and which features carry them; `.claude/context/
  INDEX.md` `Last updated` is moved.

## Decisions
- **Editing `.claude/domain/features/repo-local-audits.md`,
  `.claude/domain/features/pipeline-suggest.md` and
  `.claude/domain/features/shared-phase-engine.md` is explicitly authorised
  for this task (user decision, 2026-09-20), for the 3 + 1 + 1
  reconciliations listed above and for nothing else. Do not ask for
  permission at implementation time — the permission is already granted.**
  Those documents are normally `/architect`'s, and anything beyond those
  points still belongs to `/architect`. These are factual reconciliations
  with shipped behaviour, not redesigns; if one turns out to need a design
  decision rather than a wording fix, stop and say so instead of deciding it
  here.
- **No VERSION bump.** Every file here is documentation under CLAUDE.md
  § Versioning's exemption.

## Hints
- `README.md` — line ~497 (frontmatter paragraph), the `show` bullet in the
  CLI section.
- `docs/reference.md` — § `show`, § `pipeline-suggest` (lines ~769–786),
  § `runbook-suggest` (line ~1160).
- `.claude/domain/features/repo-local-audits.md`
- `.claude/domain/features/pipeline-suggest.md`
- `.claude/domain/features/shared-phase-engine.md`
- `.claude/context/cmd-show.md`, `.claude/context/features.md`,
  `.claude/context/INDEX.md`
- `docs/authoring-guide.md` § `description` — the contract to describe;
  read-only here, task 240 owns the edit.
